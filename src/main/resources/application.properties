# Connection url for the database test
spring.datasource.url = jdbc:mysql://localhost:3306/town_house?useSSL=false&&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Ho_Chi_Minh

# Username and password
spring.datasource.username = root
spring.datasource.password =

# Hibernate
spring.jpa.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.show_sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto = update

logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

#Make sure less secure App is enabled at link https://myaccount.google.com/lesssecureapps
spring.mail.host = smtp.gmail.com
spring.mail.username = townhouseapi@gmail.com
spring.mail.password = townhouseapi1999-G

#Below Properties are needed for maintaining the session.
#Two Step verification should not be enabled for gmail.
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.socketFactory.port = 465
spring.mail.properties.mail.smtp.socketFactory.class = javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback = false


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=300MB
# Max Request Size
spring.servlet.multipart.max-request-size=315MB

## Jackson Properties
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
spring.jackson.time-zone= Asia/Ho_Chi_Minh
spring.jackson.serialization.fail-on-empty-beans=false

spring.resources.cache.cachecontrol.max-age=1


#deploy on heroku

#spring.datasource.url=${JDBC_DATABASE_URL}
#spring.datasource.username=${JDBC_DATABASE_USERNAME}
#spring.datasource.password=${JDBC_DATABASE_PASSWORD}
#spring.jpa.show-sql=false
#spring.jpa.generate-ddl=true
#spring.jpa.hibernate.ddl-auto=update
#
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
#spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
#spring.datasource.hikari.autocommit: false

#Don't forget change pom.xml file and UserServiceImpl file
#Change spring.jpa.hibernate.ddl-auto = update to create-drop, then change to update
#Setup heroku to local here: https://devcenter.heroku.com/articles/heroku-postgresql
#Task in internship:
#Test all function
#Auto update data when time change
#Complete booking and send email for host and user of send email for both if booking cancel
#Flash sale and discount code
#Login with Google or Facebook
#Filter and serching hosts
#Payment online